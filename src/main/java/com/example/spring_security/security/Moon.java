package com.example.spring_security.security;

import org.springframework.security.authentication.UsernamePasswordAuthenticationToken;
import org.springframework.security.core.GrantedAuthority;
import org.springframework.security.core.userdetails.UserDetails;

import java.util.Collection;

//AUTHENTICATIONMANAGER


//AUTHENTICATION_PROVIDER
    //DaoAuthenticationProvider(supports, authenticate)

//    USER_DETAILS_SERVICE
    //UserDetails()
    //User (username, password, roles)
    //UserDetailsService(loaduserbyusername)
    //UserDetailsManager(createUser, updateUser, changePassword, deleteuser, getroles)
    //InMemoryUserDetailsManager(implementation of UserDetailsmnager and userdetailsservice)
    //JDBCUserDetailsManager(implementation of UserDetailsmnager and userdetailsservice)
    //LDAPUserDetailsManager(implementation of UserDetailsmnager and userdetailsservice)

//    PASSWORD_ENCODER
    //NoOpPasswordEncoder
    //BcryptPasswordEncoder
    //ScryptPasswordEncoder
public class Moon {

}
